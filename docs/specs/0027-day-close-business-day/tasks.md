# 0027 — Tasks

- [x] `DayCloseService` takes `BusinessDayCalendar`; the guard compares business day to business day
- [x] Construction sites updated: DI (automatic), `HistoryGenerator`, two test classes
- [x] `DayCloseBoundaryTests` — a **fixed** clock at 01:30 Asia/Dhaka proving both directions
- [x] The four time-dependent integration failures are gone; full suite green at 01:30 Dhaka
