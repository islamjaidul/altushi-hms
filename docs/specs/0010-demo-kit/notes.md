# 0010 — Notes
- 2026-07-26 — Reset/snapshot flow, dual instances, demo cast and the offline posture (all assets
  self-hosted; CI no-external-hosts gate) are in place. The 90-day history generator and the
  25–40-operator load measurement need the demo laptop / target-class VM plus the screen layer —
  both recorded as the sprint's remaining exit items, per 08 §S6. The 06 §2 budget table therefore
  REMAINS ESTIMATES (explicitly not measured — DoD item open).
