@{
    // Mirrors src/Hms.Web/Pages/_ViewStart.cshtml. The root page only redirects today, so nothing
    // renders through it — but a host whose Pages folder has no _ViewStart is one added screen away
    // from the same unstyled-fragment bug.
    Layout = "_Layout";
}
